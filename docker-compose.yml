
services:

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - ai
    restart: always

  ai:
    build: ./ai-service
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: always